# -*- mode:org -*-                                                                                                                                    
* Recursion
  1. Factorial
     Calculate the factorial of a given number using recursion
  2. GCD
     Calculate the gcd of two given numbers using recursion
  3. Fibonnaci sequence
     1. Print the Fibonnaci sequence using recursion
     2. Print nth number in the Fibonnaci sequence using recursion
  4. Tower of Hanoi
     1. Calculate the number of steps required for n ring tower
     2. Print the sequence of steps for an n ring towerg
  5. Write program to calculate the sum of all the digits of a given number using recursion
  6. Write program to reverse the digits of a given number using recursion

* Arrays
  1. Dynamic Arrays Programs: traverse, insert, delete, search, sort
  2. 2d array:: traverse, insert, delete, search, sort
  3. 3d array:: traverse, insert, delete, search, sort
  4. Missing number:
     Given unsorted array of size N, its filled with numbers from 1 to N+1 without repetition, find the no which
     is missing

     Example
     Input: (4,2,1,3,6]
     Output: 5
  5. Largest Element Towards Left & Right - Problem Statement:
     Find the largest element towards left and right of each element in an unsorted array. If there are no
     element larger than current element, fill it with -1

     Example
     Input: (4,2,1,3,6,4]

     Output:
     Largest element on left [-1,4,4,4,-1,6]
     Largest element on right [6,6,6,6,-1,-1]
  6. Reverse an array
  7. Shift Array By K-Places -
     Shift an array by K places towards left

     Example
     Input: [1,2,3,4,5,6,7] k=2
     Output: [3,4,5,6,7,1,2]
  8. Stock Price Problem
     Given prices of stocks priceli] on i th day, could you compute the max profit that could be computed by doing only 1 transaction.

     Example
     Input: prices] ={7, 1,5, 3,6, 4]
     Output: 5
  9. Water Trapping -
     Given n non-negative integers representing height of the building, compute how much water it is able to
     trap after raining.

       Input: arr[] = {3,0, 2,0, 4}
       Output: 7
  10. Equilibrium index
      Equilibrium index of an array is an index such that the sum of elements at lower indexes is equal to the
      sum of elements at higher indexes.

      Example
      Input: nums = [1,1,1]
      Output: 2
  11. Set Matrix Zero -
      Given an array of intervals where intervals[i] = [starti, endil, merge all overlapping intervals, and return an

      array of the non-overlapping intervals that cover all the intervals in the input.

      Example
      Input: intervals = [[1,3],[2,61,[8,101,[15,18]]
      Output: [[1,6],(8,10],[15,18]]
  12. Rotate Image 
      You are given an n x n 2D matrix representing an image, rotate the image by 90 degrees (clockwise).

      Example
      Input: matrix = ([1,2,3,14,5,61,(7,8,91]
      Output: [[7,4,1],[8,5,21,9,6,31]
  13. Extra- Queries on a matrix 
      Given a matrix M of size mx n ( 1 <=m,n <= 1000 ). Its initially filled with integers from 1 to mx n
      sequentially in a row major order. The task is to process a list of queries manipulating M such that every
      query is one of the following three.

      R(x, y): swaps the x-th and y-th rows of M where x and y vary from 1 to m.

      C(x, y): swaps the x-th and y-th columns of M where x and y vary from 1 to n.

      P(x, y): prints the element at x-th row and y-th column where x varies from 1 to m and y varies from 1 to n.

      Input:m=4,n=4
      Queries: R(1, 3)
      P(1,1)
      Output: 9 ns

* Linked List
** Notes
   - slow and fast pointer
** 
* Searching and sorting
** Searching
   1. Implement linear search on an arraay in java
   2. Implement binary search on an arraay in java
** sorting
*** Insertion sort
    1. Implement insertion sort for numerical array input
    2. Implement all the sorting algorithms on linked list
*** Other Problems
    1. Write a program to calculate the minimum number of swaps to sort the array
    2. Write a program to Sort an array in wave form
    3. Given an array of positive and negative numbers, arrange them in an alternate fashion such that
       every positive number is followed by a negative and vice-versa maintaining the order of
       appearance.

       Examples
       Input: 1,2,3,-4,-1,4
       Output: -4,1,-1,2,3,4
    4. Question 2: Median Sort
       Given are some integers, which are read from the data stream. The task is to find the median of the
       integers read so far.

       The median is the middle value of a sorted list of integers. If the size of the list is even, the median is
       the average of the two middle elements.

       Example

       Input: 1,2, 3,...

       Output: 1,1.5,2...
    5. Pairs with Min Diff
       Given an array of distinct integers arr, find all pairs of elements with the minimum absolute difference of any two elements.
       Return a list of pairs in ascending order(with respect to pairs), each pair [a, b] follows.
       a,b are from arr
       a<b
       b- a equals to the minimum absolute difference of any two elements in arr
       Example
       Input; arr = [4.2,1,3]
       Output: [[1,2],(2,3][34]]
    6. Search in Rotated Sorted Array
       There is an integer array nums sorted in ascending order (with distinct values).

       Given the array nums after the possible rotation and an integer target, return the index of target if it is in
       nums, or -1 if itis not in num.

       Example

       Input: nums = [4,5,6,7,0,1,2), target = 0

       Output: 4
    7. Find first and last Position of Element in Sorted Array

       Given an array of integers sorted in non-decreasing order, find the starting and ending position of
       a given target value.

       If target is not found in the array, return [-1, -1].

       Example

       Input: nums =[5,7,7,8,8,10], target = 8

       Output: [3,4]
    8. Find first and last Position of Element in Sorted Array
       Given an array of integers sorted in non-decreasing order, find the starting and ending position of
       a given target value.

       If target is not found in the array, return [-1, -1].

       Example

       Input: nums =[5,7,7,8,8,10], target = 8

       Output: [3,4]
    9. Kth Largest Element in an Array
       Given an integer array input and an integer k, return the kth largest element in the array.
       Example

       Input: arr = [32,1,5,6,4] k = 2

       Output: 5
    10. 
* String
** Notes
** Problems
   1. KMP algorithm
      Implement string pattern search using KMP algorithm
   2. Rabin Karp algorithm
      Implement string pattern search using Rabin Karp algorithm
   3. Longest Palindromic Substring
      1. Implement using brute force approach
      2. Implemt using optimised approach: moving mirror pointer approach
   4. https://leetcode.com/problems/roman-to-integer/
   5. Check wheather two strings are anagrams of each other.
   6. Find the longest substring with k unique characters
   7. Compare version numbers:
      Given 2 revision, your task is to compare version numbers.
      Version number consist of set separated by dots. The versions are moved from left to right.
      If version1< version2, return -1 else return +1.

      Example 1:

      Input:

      versionl = "1.00001", version2="1.001"
      Output: 0

      Example 2:

      Input:
      versionl = "1.0.0001", version2="1.1.001"
      Output: -1

   8. Count and say
      Your task is to implement count and say.

      For n=1, it should print "1". This is the base case.
      For n=2, it should print "1:
      For n=3, it should print "2:
      For n=4, it should print "1211"


   9. 
* Sets
  - For Hashset, LinkedHashset, TreeSwweet
    1. implement basiic traverse, insert, delete, search, sort
       - verify duplicate, null input value and ordering constraint
  - Use cases: Solve the problems in the course
  - Read the hashing article in the ticket and reaattempt th test
** Hashset
   1. implement basiic traverse, insert, delete, search, sort
      - verify duplicate and ordering constraint

* Stacks
  - Implement infix to postfix/ prefix using java staCK CLASS
* Trees
  1. Implement a generic class that allows the user to choose the implementation backed by arrays or linked lists
     - implement basic traverse, insert, delete, search, sort
       - implement inorder, pre/post order traversal
       - implement level by lebvel traversal
	 - iteratively and recursivly
       - implement boundry traversal
     - implement methods to get
       - max height given number of nodes
       - max nodes given height
     -   
* Binary trees
  1. Implement a generic class that allows the user to choose the implementation backed by arrays or linked lists
     - implement basic traverse, insert, delete, search, sort for BST
       - implement inorder, pre/post order traversal
       - implement level by lebvel traversal
	 - iteratively and recursivly
       - implement boundry traversal
       - implement the delete algorithm in bst
	 - when the node to be deleted has a right and left subbtree
	 - when it has only left or right tree
     - implement methods to get
       - max height given number of nodes
       - max nodes given height
     - 
  2. Implement the range sum problrem
     https://olympus.mygreatlearning.com/courses/94760/pages/range-sum?module_item_id=4296507
  3. 
     https://olympus.mygreatlearning.com/courses/94760/pages/sorted-array-to-bst?module_item_id=4296508
  4. 
     https://olympus.mygreatlearning.com/courses/94760/pages/morris-traversal?module_item_id=4296509
  5.
     https://olympus.mygreatlearning.com/courses/94760/pages/bst-kth-smallest-element?module_item_id=4296510
  6. 

** 
* Heaps
  1. implement heap backed by arrays
     1. implement max and min heaps
     2. implement the insertion and deleteion algorithms in min and max heaps
     3. traverse, insert, delete, search, sort on both min and max heap
     4. implement code to convert an array into min/max heap
	1. use heap to sort a given array -> heapify it then delete each element one by one
* Queues
  1. Implement the flood fill algorithm using the Breadth First and Depth First approach.
* 
